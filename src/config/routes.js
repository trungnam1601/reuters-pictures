const routes = {
    home: '/',
    login: '/login',
    follow: '/follow-us',
    register: '/register',
    picturePackage: '/picture-packages',
    search: '/package/search/:keyword',
    searchDetail: '/package/search/:keyword/:detail',
};

export default routes;
